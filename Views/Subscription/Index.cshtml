@model List<LibraryMPT.Models.Subscription>

@{
    ViewBag.Title = "Управление подписками";
    Layout = "_Layout";
    var templates = ViewBag.Templates as List<LibraryMPT.Models.Subscription> ?? new List<LibraryMPT.Models.Subscription>();
    var activeSubscriptions = ViewBag.ActiveSubscriptions as List<LibraryMPT.Models.Subscription> ?? new List<LibraryMPT.Models.Subscription>();
}

<div class="container fade-in">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>
            <i class="fas fa-id-card me-2"></i>Управление подписками
        </h2>
        <div class="d-flex gap-2">
            <a asp-action="Index" asp-controller="Admin" class="btn btn-outline-primary">
                <i class="fas fa-arrow-left"></i> Назад в панель
            </a>
            <a asp-action="Create" class="btn btn-success">
                <i class="fas fa-plus"></i> Создать шаблон подписки
            </a>
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <h3 class="mb-3">
        <i class="fas fa-file-alt me-2"></i>Шаблоны подписок
    </h3>
    <p class="text-muted mb-4">Шаблоны подписок, которые могут выбрать представители учебных заведений</p>

    @if (!templates.Any())
    {
        <div class="alert alert-info">
            <i class="fas fa-info-circle me-2"></i>
            Шаблоны подписок отсутствуют. Создайте первый шаблон подписки.
        </div>
    }
    else
    {
        <div class="table-responsive mb-5">
            <table class="table table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th>Название</th>
                        <th>Длительность</th>
                        <th class="text-end">Действия</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var template in templates)
                    {
                        <tr>
                            <td>
                                <strong>@template.Name</strong>
                            </td>
                            <td>
                                @if (template.DurationDays.HasValue)
                                {
                                    var days = template.DurationDays.Value;
                                    var months = days / 30;
                                    var years = days / 365;
                                    
                                    if (years >= 1)
                                    {
                                        <span class="badge bg-info">@years @(years == 1 ? "год" : years < 5 ? "года" : "лет") (@days дней)</span>
                                    }
                                    else if (months >= 1)
                                    {
                                        <span class="badge bg-info">@months @(months == 1 ? "месяц" : months < 5 ? "месяца" : "месяцев") (@days дней)</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-info">@days @(days == 1 ? "день" : days < 5 ? "дня" : "дней")</span>
                                    }
                                }
                            </td>
                            <td class="text-end">
                                <form asp-action="Delete" asp-route-id="@template.SubscriptionID" 
                                      method="post" 
                                      onsubmit="return confirm('Удалить шаблон подписки?');"
                                      class="d-inline">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-sm btn-danger">
                                        <i class="fas fa-trash"></i> Удалить
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }

    <h3 class="mb-3">
        <i class="fas fa-check-circle me-2"></i>Активные подписки учебных заведений
    </h3>
    <p class="text-muted mb-4">Подписки, активированные представителями учебных заведений</p>

    @if (!activeSubscriptions.Any())
    {
        <div class="alert alert-info">
            <i class="fas fa-info-circle me-2"></i>
            Активные подписки отсутствуют.
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th>Название</th>
                        <th>Учебное заведение</th>
                        <th>Дата начала</th>
                        <th>Дата окончания</th>
                        <th>Статус</th>
                        <th class="text-end">Действия</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var sub in activeSubscriptions)
                    {
                        var isActive = sub.StartDate.HasValue && sub.EndDate.HasValue && 
                                       DateTime.Now >= sub.StartDate.Value && DateTime.Now <= sub.EndDate.Value;
                        <tr>
                            <td>
                                <strong>@sub.Name</strong>
                            </td>
                            <td>
                                @(sub.Faculty?.FacultyName ?? "Не указано")
                            </td>
                            <td>
                                @(sub.StartDate?.ToString("dd.MM.yyyy") ?? "-")
                            </td>
                            <td>
                                @(sub.EndDate?.ToString("dd.MM.yyyy") ?? "-")
                            </td>
                            <td>
                                @if (isActive)
                                {
                                    <span class="badge bg-success">
                                        <i class="fas fa-check-circle"></i> Активна
                                    </span>
                                }
                                else if (sub.StartDate.HasValue && DateTime.Now < sub.StartDate.Value)
                                {
                                    <span class="badge bg-warning">
                                        <i class="fas fa-clock"></i> Не началась
                                    </span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">
                                        <i class="fas fa-times-circle"></i> Истекла
                                    </span>
                                }
                            </td>
                            <td class="text-end">
                                <form asp-action="Delete" asp-route-id="@sub.SubscriptionID" 
                                      method="post" 
                                      onsubmit="return confirm('Удалить подписку?');"
                                      class="d-inline">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-sm btn-danger">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>

